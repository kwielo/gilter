{
  "name": "gilter",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Gmail filter editor — import, view, edit, and export Gmail filters XML",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "27.0.0",
    "typescript": "6.0.3",
    "vite": "6.4.3",
    "vitest": "4.1.10"
  }
}
